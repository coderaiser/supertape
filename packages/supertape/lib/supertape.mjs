import test from './supertape.js';

const {
    extend,
    stub,
    createStream,
    init,
    run,
    createTest,
} = test;

export default test;

export {
    extend,
    test,
    stub,
    createStream,
    init,
    run,
    createTest,
};
